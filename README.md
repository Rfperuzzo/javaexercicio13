Exercício com IF em Java 4(ou sei lá já perdi a conta) A VINGANÇA ! Exercício simples desenvolvido em Java para praticar o uso de estruturas condicionais if e else.

Objetivo Praticar conceitos básicos de programação, como:

Entrada de dados Variáveis Operadores de comparação Estruturas condicionais if e else Exibição de resultados Tecnologias Java Scanner para entrada de dados Execução Compile o arquivo:

javac Main.java Depois execute:

java Main Autor Ramon
